import React from 'react';
import Layout from "@/components/Layout";

const Settings = () => {
    return (
        <div>
            <Layout>
                <h1 className="title-dashboard mb-2">Settings</h1>
            </Layout>

        </div>
    );
};

export default Settings;